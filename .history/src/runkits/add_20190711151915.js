const code = `function add (x, y) {
  return x + y
}

console.log(add(5, 7)) // returns 12
console.log(add(5, 7)) // still returns 12
console.log(add(5, 7)) // still returns 12!`

const imports = ``

const requires = ``

export default {
  code,
  imports,
  requires
}
