const code = `function areaFromDiameter (diameter) {
  const radius = diameter / 2
  
  return Math.PI * radius * radius
}

areaFromDiameter(2) // Radius is 1, 1 squared is 1, area is PI
`

const imports = ``

const requires = ``

export default {
  code,
  imports,
  requires
}
