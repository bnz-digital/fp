import { Link } from 'gatsby'

**Functional programming in JavaScript is easy.** Easier, in fact, than OOP in JavaScript. Simpler. Cleaner.

So why do so many OO programmers find learning FP so difficult? In part, it's because they search online and what they come across is _academic_ discussions of the theory behind functional programming: [Category Theory](https://en.wikipedia.org/wiki/Category_theory), one of the more advanced and difficult of the maths disciplines. And in these discussions they discover a host of obscure terms, such as _lambda calculus_, _referential transparency_, _recursion_, and the dreaded _monad_.

These concepts are difficult even for academics. The good news is that **you don't have to understand Category Theory or know  what any of these terms mean to write good functional code**. The way the code is written is quite simple and easy (until it isn't, but that's true of OOP as well, isn't it?).

Here we present the fundamental ideas behind functional programming without all the boring, academic details and terminology. You, too, can FP.

## How to FP

<ol>
  <li>
    <Link to="/reusability/" aria-label="Reusability">Functions are the basic unit of reusability</Link>
    <ol>
      <li>Functions take at least one argument</li>
      <li>Functions return exactly one result</li>
      <li>Functions can be passed as arguments to other functions</li>
      <li>Functions can be returned from other functions</li>
      <li>Functions can call functions, <em>including themselves</em></li>
      <li>Given the same arguments, functions must return the same result</li>
      <li>Don't be reaching outside the function to mess with shit</li>
      <li>Make copies when passing by reference</li>
      <li>Functions can remember things for you</li>
    </ol>
  </li>
  <li>
    <Link to="/immutability/" aria-label="Immutability">Variables are &ldquo;set and forget&rdquo;</Link>
    <ol>
      <li>Use <code>const</code>!</li>
      <li>Assign variables when they are declared</li>
      <li>Once a variable has been assigned, don't be changing it</li>
      <li>Don't erase state: create new state instead!</li>
      <li>Unreassignable !== immutable</li>
      <li>Use destructuring and the rest/spread operators to make copies</li>
    </ol>
  </li>
  <li>
    <Link to="/iteration/" aria-label="Iteration">Loops are so last century</Link>
    <ol>
      <li>Functions can call themselves! No, really...</li>
      <li>Use <code>map</code> to modify each value in a list</li>
      <li>Use <code>filter</code> to choose particular values from a list</li>
      <li>Use <code>adjust</code> to update a single value in a list</li>
      <li>Use <code>append</code>, <code>insert</code>, or <code>prepend</code> to add an item to a list</li>
      <li>Use <code>concat</code> or <code>zip</code> to combine two lists</li>
      <li>Use <code>times</code> to create a new list</li>
      <li>Use <code>reduce</code> to combine elements in a list</li>
      <li>Use <code>reduce</code> to convert a list to some other data type</li>
      <li>Use <code>head</code> or destructuring to get the first item in a list</li>
      <li>Use <code>tail</code> or destructuring to get the list minus the first element</li>
      <li>Use <code>all</code>, <code>any</code>, or <code>none</code> to test a list</li>
      <li>Use <code>flatten</code> to flatten nested lists into a single list</li>
      <li>Avoid <code>forEach</code>! Avoid <code>forEach</code>! Avoid <code>forEach</code>!</li>
    </ol>
  </li>
  <li>
    <Link to="/composition/" aria-label="Composition">We can has pipelines!</Link>
    <ol>
      <li>Use <code>pipe</code> or <code>compose</code> to combine functions into pipelines</li>
      <li>Use <code>curry</code> to make functions that make functions</li>
      <li>Think in terms of state!</li>
    </ol>
  </li>
  <li>
    <Link to="/patterns-antipatterns/" aria-label="Patterns and anti-patterns">Patterns and anti-patterns</Link>
    <ol>
      <li>Do this not that</li>
      <li>Do this not that</li>
      <li>Do this not that</li>
      <li>Do this not that</li>
      <li>Do this not that</li>
    </ol>
  </li>
</ol>
