const code = `const { insert } = require('ramda')

const names = [
  'Bob', 'Carol', 'Ted', 'Samson', 'Delilah'
]

// Ramda
insert(3, 'Alice', names)`

const imports = ``

const requires = ``

export default {
  code,
  imports,
  requires
}
